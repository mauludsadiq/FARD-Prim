name: FARD Prim Regression Suite

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  regression:
    name: Regression (Linux ARM64)
    runs-on: ubuntu-24.04-arm

    steps:
      - uses: actions/checkout@v4

      - name: Install fardrun
        run: |
          cp ci/fardrun-linux-arm64 /usr/local/bin/fardrun
          chmod +x /usr/local/bin/fardrun
          fardrun --version

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Create output directory
        run: mkdir -p out

      - name: Run FARD regression suite (ARM64)
        run: |
          fardrun run --program programs/regression_arm64.fard --out /tmp/reg
          chmod +x out/r_* 2>/dev/null || true
          python3 programs/regression_run.py /tmp/reg/result.json

      - name: Run Python regression suite
        run: python3 programs/regression_python.py

      - name: Upload artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: regression-output
          path: out/

      - name: Debug while loop binary
        if: always()
        run: |
          python3 programs/compile_python.py 'def f():\n  i = 0\n  while i < 3:\n    i = i + 1\n  return i\nf()' out/debug_while || true
          if [ -f out/debug_while ]; then
            file out/debug_while
            timeout 2 out/debug_while; echo "exit=$?"
          fi

      - name: Debug while disasm
        if: always()
        run: |
          python3 programs/compile_python.py 'def f():\n  i = 0\n  while i < 3:\n    i = i + 1\n  return i\nf()' out/debug_while2 || true
          objdump -d out/debug_while2 2>/dev/null | head -100 || true
